PORT=5000
NODE_ENV=development
# Local:
# MONGODB_URI=mongodb://127.0.0.1:27017/ai_interior_design_mvp
#
# Atlas: If mongodb+srv querySrv ECONNREFUSED occurs, avoid using the SRV connection — use the 
# Compass / seed host + directConnection=true (see project .env comment) or a "standard" connection string from Atlas.
JWT_SECRET=change_me_change_me


